# Vite UI environment variables

# Base URL for Laravel API. Default '/api'.
# In production builds, this must be non-empty.
VITE_API_BASE=/api

# Dev server-only: where Vite proxies API requests during development.
# This should point directly at the Laravel backend service.
# docker-compose.dev.yml sets this to http://web (the Laravel container).
VITE_API_PROXY_TARGET=http://localhost:8000

