import { PATHS } from '../routes/paths';

export const NAV_ITEMS = [
  { to: PATHS.ROOT, label: 'Home', end: true },
  { to: PATHS.DISCOVER, label: 'Discover' },
  { to: PATHS.DASHBOARD.ROOT, label: 'Dashboard' },
];
