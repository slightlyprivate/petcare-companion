name: Build Develop Images

on:
  push:
    branches: [develop]
    paths:
      - 'src/**'
      - 'docker/**'
      - 'docker-bake.hcl'
      - '.github/workflows/build-develop-images.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute build version
        id: version
        run: |
          VERSION=$(git describe --tags --dirty --always 2>/dev/null || git rev-parse --short HEAD)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "VITE_APP_VERSION=$VERSION" >> $GITHUB_ENV

      - name: Compute short SHA
        id: shortsha
        run: echo "SHORT_SHA=$(git rev-parse --short=7 HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push develop images
        uses: docker/bake-action@v5
        with:
          files: ./docker-bake.hcl
          targets: develop
          push: true
          set: |
            *.cache-from=type=gha
            *.cache-to=type=gha,mode=max
            app-dev.tags=ghcr.io/slightlyprivate/petcare-companion-app:develop
            app-dev.tags+=ghcr.io/slightlyprivate/petcare-companion-app:dev-${{ steps.shortsha.outputs.SHORT_SHA }}
            web.tags=ghcr.io/slightlyprivate/petcare-companion-web:develop
            web.tags+=ghcr.io/slightlyprivate/petcare-companion-web:dev-${{ steps.shortsha.outputs.SHORT_SHA }}
            ui.tags=ghcr.io/slightlyprivate/petcare-companion-ui:develop
            ui.tags+=ghcr.io/slightlyprivate/petcare-companion-ui:dev-${{ steps.shortsha.outputs.SHORT_SHA }}
            pwa.tags=ghcr.io/slightlyprivate/petcare-companion-pwa:develop
            pwa.tags+=ghcr.io/slightlyprivate/petcare-companion-pwa:dev-${{ steps.shortsha.outputs.SHORT_SHA }}
            ui.args.VITE_APP_VERSION=${{ steps.version.outputs.version }}
            pwa.args.VITE_APP_VERSION=${{ steps.version.outputs.version }}
