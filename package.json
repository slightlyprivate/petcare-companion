{
  "name": "petcare-companion-root",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "src/ui",
    "src/pwa"
  ],
  "scripts": {
    "prepare": "husky install",
    "format:check": "npm run format:check --workspaces",
    "format": "npm run format --workspaces"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "lint-staged": "^15.2.7",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,md,json,yaml,yml,html}": [
      "prettier --write"
    ]
  }
}
